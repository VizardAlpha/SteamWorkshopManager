<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SteamWorkshopManager.ViewModels"
             xmlns:models="using:SteamWorkshopManager.Models"
             xmlns:local="using:SteamWorkshopManager"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="400"
             x:Class="SteamWorkshopManager.Views.SettingsView"
             x:DataType="vm:SettingsViewModel">

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Border Grid.Row="0" Padding="24,16" Background="#171a21">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Button Command="{Binding CloseCommand}"
                        Classes="ghost">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="&#x2190;" FontSize="16"/>
                        <TextBlock Text="{DynamicResource Back}"/>
                    </StackPanel>
                </Button>
                <TextBlock Grid.Column="1" Text="{DynamicResource Settings}"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center" Margin="24,0"/>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="24" Margin="24,24">
                <!-- Sessions Section -->
                <Border Classes="card">
                    <StackPanel Spacing="16">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="{DynamicResource Sessions}" Classes="section-header"/>
                            <Button Grid.Column="1"
                                    Command="{Binding AddSessionCommand}"
                                    Classes="accent"
                                    Padding="12,6">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="+" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock Text="{DynamicResource AddSession}"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- Sessions List -->
                        <ItemsControl ItemsSource="{Binding Sessions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:WorkshopSession">
                                    <Button Command="{Binding $parent[ItemsControl].((vm:SettingsViewModel)DataContext).SwitchSessionCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Padding="0"
                                            Margin="0,0,0,8"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <Border Padding="12"
                                                CornerRadius="6"
                                                Background="#1e2328">
                                            <Grid ColumnDefinitions="Auto,*,Auto">
                                                <!-- Active indicator -->
                                                <Border Grid.Column="0"
                                                        Width="4"
                                                        CornerRadius="2"
                                                        Margin="0,0,12,0"
                                                        Background="#a4d007"
                                                        IsVisible="{Binding IsActive}"/>

                                                <!-- Session info -->
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="{Binding GameName}"
                                                               FontWeight="SemiBold"
                                                               Foreground="White"/>
                                                    <TextBlock Text="{Binding AppId, StringFormat='AppID: {0}'}"
                                                               FontSize="12"
                                                               Foreground="#8899a6"/>
                                                </StackPanel>

                                                <!-- Active badge -->
                                                <Border Grid.Column="2"
                                                        IsVisible="{Binding IsActive}"
                                                        Background="#2a3a2a"
                                                        CornerRadius="4"
                                                        Padding="8,4"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{DynamicResource Active}"
                                                               FontSize="11"
                                                               Foreground="#a4d007"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Restart notice -->
                        <Border Background="#1a3a5a"
                                CornerRadius="4"
                                Padding="12">
                            <TextBlock Text="{DynamicResource SessionSwitchNotice}"
                                       FontSize="12"
                                       Foreground="#66c0f4"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Language Section -->
                <Border Classes="card">
                    <StackPanel Spacing="16">
                        <TextBlock Text="{DynamicResource Language}" Classes="section-header"/>

                        <ListBox ItemsSource="{Binding AvailableLanguages}"
                                 SelectedItem="{Binding SelectedLanguage}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.Styles>
                                <Style Selector="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,16,0"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                                <Style Selector="ListBoxItem /template/ ContentPresenter">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                                <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                                <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                                <!-- Unselected card -->
                                <Style Selector="ListBoxItem Border.lang-card">
                                    <Setter Property="BorderBrush" Value="#3a4a5a"/>
                                    <Setter Property="Background" Value="#1e2328"/>
                                </Style>
                                <Style Selector="ListBoxItem TextBlock.lang-code">
                                    <Setter Property="Foreground" Value="#8899a6"/>
                                </Style>
                                <!-- Selected card -->
                                <Style Selector="ListBoxItem:selected Border.lang-card">
                                    <Setter Property="BorderBrush" Value="#a4d007"/>
                                    <Setter Property="Background" Value="#2a3a2a"/>
                                </Style>
                                <Style Selector="ListBoxItem:selected TextBlock.lang-code">
                                    <Setter Property="Foreground" Value="#a4d007"/>
                                </Style>
                            </ListBox.Styles>
                            <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="models:LanguageInfo">
                                    <Border Classes="lang-card" Width="140" Height="90"
                                            CornerRadius="8" ClipToBounds="True"
                                            BorderThickness="3" Cursor="Hand">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
                                            <TextBlock Classes="lang-code" Text="{Binding CountryCode}"
                                                       FontSize="24" HorizontalAlignment="Center" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding NativeName}"
                                                       Foreground="White"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

                <!-- Debug Mode Section -->
                <Border Classes="card">
                    <StackPanel Spacing="16">
                        <TextBlock Text="{DynamicResource DebugMode}" Classes="section-header"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{DynamicResource EnableDebugMode}"
                                           Foreground="White"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{DynamicResource DebugModeDescription}"
                                           Foreground="#8899a6"
                                           FontSize="12"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                            <ToggleSwitch Grid.Column="1"
                                          IsChecked="{Binding IsDebugModeEnabled}"
                                          OnContent=""
                                          OffContent=""
                                          VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Log file path (visible when debug mode is enabled) -->
                        <StackPanel IsVisible="{Binding IsDebugModeEnabled}" Spacing="8">
                            <TextBlock Text="{DynamicResource LogFileLocation}"
                                       Foreground="#8899a6"
                                       FontSize="12"/>
                            <Button Command="{Binding OpenLogFolderCommand}"
                                    Padding="0"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    HorizontalAlignment="Left">
                                <Border Background="#1e2328"
                                        CornerRadius="4"
                                        Padding="12,8">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="📂" FontSize="14" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding LogFilePath}"
                                                   Foreground="#66c0f4"
                                                   FontSize="11"
                                                   FontFamily="Consolas, Courier New, monospace"
                                                   TextWrapping="Wrap"
                                                   TextDecorations="Underline"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- App Info -->
                <Border Classes="card">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Steam Workshop Manager"
                                   FontSize="16" FontWeight="SemiBold" Foreground="White"/>

                        <!-- Version + update info -->
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding Source={x:Static local:AppInfo.Version}, StringFormat='Version {0}'}"
                                       FontSize="12" Foreground="#8899a6"
                                       VerticalAlignment="Center"/>

                            <!-- New version available indicator -->
                            <StackPanel Orientation="Horizontal" Spacing="6"
                                        IsVisible="{Binding IsUpdateAvailable}"
                                        VerticalAlignment="Center">
                                <TextBlock Text="→" FontSize="13" Foreground="#a4d007"
                                           FontWeight="Bold" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding UpdateInfo.LatestVersion}"
                                           FontSize="12" Foreground="#a4d007"
                                           FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <Button Command="{Binding OpenReleasePageCommand}"
                                        Padding="8,3"
                                        FontSize="11"
                                        Classes="accent"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{DynamicResource Download}"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock FontSize="12" Foreground="#556677">
                            <Run Text="{Binding ActiveSession.GameName, FallbackValue='No session'}"/>
                            <Run Text=" (AppID: "/>
                            <Run Text="{Binding ActiveSession.AppId, FallbackValue='N/A'}"/>
                            <Run Text=")"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
